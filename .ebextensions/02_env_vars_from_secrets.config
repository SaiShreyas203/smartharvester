# If you prefer EB to inject environment variables referencing Secrets Manager ARNs,
# you can add them here (or set them in the EB Console environment variables).
# NOTE: It's usually simpler to set env vars via the EB Console UI.
# Replace the placeholder values if you want to set here.
option_settings:
  aws:elasticbeanstalk:application:environment:
    # example environment variables; set true ARNs or values in console instead if preferred
    DEBUG: "False"
    DJANGO_SETTINGS_MODULE: "config.settings"
    # DB_SECRET_ARN: "arn:aws:secretsmanager:REGION:ACCOUNT:secret:harvest/rds/credentials"
    # REDIS_SECRET_ARN: "arn:aws:secretsmanager:REGION:ACCOUNT:secret:harvest/redis/credentials"